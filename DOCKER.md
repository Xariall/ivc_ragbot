# 🐳 Docker инструкция

## Быстрый старт

1. **Убедитесь, что файл `.env` создан и заполнен:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env и добавьте ваши ключи
   ```

2. **Соберите и запустите контейнер:**
   ```bash
   docker-compose up -d --build
   ```

3. **Просмотрите логи:**
   ```bash
   docker-compose logs -f
   ```

4. **Остановите бота:**
   ```bash
   docker-compose down
   ```

## Полезные команды

```bash
# Перезапуск бота
docker-compose restart

# Просмотр статуса
docker-compose ps

# Просмотр логов (последние 100 строк)
docker-compose logs --tail=100

# Просмотр использования ресурсов
docker stats ivc_ragbot

# Вход в контейнер (для отладки)
docker exec -it ivc_ragbot /bin/bash

# Очистка всех данных (осторожно!)
docker-compose down -v
```

## Решение проблем

### Бот не запускается

1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что `.env` файл существует и содержит правильные ключи
3. Проверьте, что порты не заняты: `docker-compose ps`

### Нехватка памяти

Если бот падает при обработке больших PDF:
1. Увеличьте лимит памяти в `docker-compose.yml`:
   ```yaml
   limits:
     memory: 4G  # Вместо 2G
   ```
2. Пересоберите контейнер: `docker-compose up -d --build`

### Данные не сохраняются

Убедитесь, что volumes правильно смонтированы:
```bash
docker-compose config
```

Проверьте права доступа к директориям `chroma_db/` и `temp_files/`.

## Структура данных

- `chroma_db/` - база данных ChromaDB (сохраняется между перезапусками)
- `temp_files/` - временные файлы загруженных документов

Обе директории монтируются как volumes, поэтому данные сохраняются даже после удаления контейнера.

