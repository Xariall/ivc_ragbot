# RAG Чат-бот для Telegram

## 📋 Описание

Этот проект представляет собой RAG (Retrieval-Augmented Generation) чат-бота для Telegram, который позволяет пользователям загружать документы (PDF, TXT, DOCX), а затем задавать вопросы на основе содержимого этих документов.

## 🏗️ Архитектура системы

### Компоненты системы:

1. **Telegram Bot Interface** (`rag_bot/bot.py`)
   - Принимает сообщения от пользователей
   - Обрабатывает команды и загрузку файлов
   - Отправляет ответы пользователям
   - Управляет памятью диалога

2. **Document Processor** (`rag_bot/document_processor.py`)
   - Загружает документы различных форматов (PDF, TXT, DOCX)
   - Парсит текст из документов
   - Разбивает текст на чанки (chunks) для эффективного поиска
   - Подготавливает данные для создания эмбеддингов

3. **Vector Store** (`rag_bot/vector_store.py`)
   - Создает эмбеддинги текстовых чанков
   - Сохраняет эмбеддинги в ChromaDB
   - Выполняет семантический поиск релевантных чанков по запросу пользователя

4. **RAG Chain** (интегрирован в `rag_bot/bot.py`)
   - Объединяет найденные релевантные чанки с запросом пользователя
   - Использует Groq Llama для генерации ответа
   - Сохраняет историю диалога для контекста

5. **Configuration** (`rag_bot/config.py`)
   - Загружает переменные окружения
   - Настраивает параметры системы
   - Управляет конфигурацией модели и базы данных

### Поток данных:

```
Пользователь → Telegram Bot → Document Processor → Vector Store (ChromaDB)
                                                          ↓
Пользователь → Telegram Bot → Vector Store (поиск) → RAG Chain → Groq LLM → Ответ
```

## 🚀 Установка и настройка

### 1. Клонирование и переход в директорию проекта

```bash
cd /Users/asanaliesmagambetov/Documents/Project/ivc_ragbot
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Для macOS/Linux
# или
venv\Scripts\activate  # Для Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните следующие переменные:
- `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота (получите у @BotFather)
- `GROQ_API_KEY` - API ключ от Groq (получите на https://console.groq.com/)

### 5. Запуск бота

```bash
python main.py
```

## 📁 Структура проекта

```
ivc_ragbot/
├── main.py                    # Точка входа для запуска бота
├── rag_bot/                   # Основной модуль бота
│   ├── __init__.py           # Инициализация модуля
│   ├── bot.py                # Класс RAG бота и обработчики
│   ├── config.py             # Конфигурация и настройки
│   ├── document_processor.py # Обработка документов (PDF, TXT, DOCX)
│   └── vector_store.py       # Работа с ChromaDB и эмбеддингами
├── requirements.txt          # Зависимости проекта
├── .env.example              # Пример конфигурации
├── .env                      # Ваши секретные ключи (не коммитьте!)
├── .gitignore                # Игнорируемые файлы для Git
├── README.md                 # Этот файл
└── chroma_db/                # Директория для ChromaDB (создается автоматически)
```

## 🎯 Использование

1. Запустите бота командой `python main.py`
2. Найдите вашего бота в Telegram
3. Отправьте команду `/start` для начала работы
4. Загрузите документ (PDF, TXT или DOCX) - просто отправьте файл боту
5. Дождитесь подтверждения, что документ обработан
6. Задавайте вопросы по содержимому документа

## 🔧 Технические детали

- **LangChain**: Используется для создания цепочек обработки, работы с LLM и управления памятью
- **ChromaDB**: Векторная база данных для хранения эмбеддингов
- **Groq**: Быстрая LLM для генерации ответов
- **python-telegram-bot**: Библиотека для работы с Telegram Bot API

## 📝 Примечания

- Документы обрабатываются асинхронно
- Каждый пользователь имеет свою изолированную базу данных документов
- Память диалога сохраняется в течение сессии
- Рекомендуемый размер чанка: 1000 символов с перекрытием 200 символов

